using UnityEngine;
using UnityEditor;
using UITKsharp;
using UnityEngine.UIElements;
using UITKsharp.Editor;
public class TestEditor : MonoBehaviour
{
    /*
    [MenuItem("UTKSharp/TestCreateTemplate")]
    static void CreateTemplate()
    {
        UISharpEditorUtil.SaveTemplate(new UISharp(new VisualElement()).Size(200, 400, true).Name("customTemplate"));
        var t = UISharpEditorUtil.GetScriptableTemplate();

        foreach(var h in t.templates)
        {
            Debug.Log("Test Height :" + h.styleInterface.height + "Test Width :" + h.styleInterface.width);
        }
    }

    [MenuItem("UTKSharp/TestCopyTemplate")]
    static void StyleTemplate()
    {
        var t = new UISharp(new VisualElement()).CopyTemplate("customTemplate");
    }
    */
}
